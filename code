<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zusammen seit</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            color: #fff;
            background-color: #2c3e50;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        header {
            background: url('Snapchat-238578772.jpg') no-repeat center center;
            background-size: cover;
            height: 70vh;
            width: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            color: #fff;
            text-shadow: 0px 4px 6px rgba(0, 0, 0, 0.7);
        }

        header h1 {
            font-size: 2rem;
            text-align: center;
            margin: 0;
            padding: 10px;
            background: rgba(0, 0, 0, 0.5); /* Dunkler Hintergrund für bessere Lesbarkeit */
            border-radius: 10px;
        }

        .timer {
            text-align: center;
            padding: 20px;
        }

        .timer h2 {
            font-size: 1.8rem;
            margin-bottom: 10px;
        }

        .timer p {
            font-size: 1.2rem;
            margin: 0;
        }

        @media (max-width: 600px) {
            header h1 {
                font-size: 1.5rem;
            }

            .timer h2 {
                font-size: 1.4rem;
            }

            .timer p {
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <header>
        <h1>Zusammen seit dem 5. Oktober 2024, 8:00 Uhr</h1>
    </header>
    <div class="timer">
        <h2>Verstrichene Zeit:</h2>
        <p id="counter">Lade...</p>
    </div>
    <script>
        function updateTimer() {
            const startDate = new Date("2024-10-05T08:00:00");
            const now = new Date();
            const diff = now - startDate;

            const days = Math.floor(diff / (1000 * 60 * 60 * 24));
